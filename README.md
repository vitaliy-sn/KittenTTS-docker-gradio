# KittenTTS Docker with Gradio

This project provides a Dockerized version of [KittenTTS](https://github.com/KittenML/KittenTTS) with a Gradio web interface.

## Quick Start

Build and run the Docker container in detached mode:

```bash
docker compose up -d --build
```
